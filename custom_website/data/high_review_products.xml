<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Create Filter Record -->
        <record id="high_review_products_filter" model="ir.filters">
            <field name="name">High Review Products</field>
            <field name="model_id">product.product</field>
            <field name="user_ids" eval="False"/>
            <field name="domain">[('high_review_product', '=', True)]</field>
            <field name="sort">["sequence asc"]</field>
        </record>

        <!-- Create Best Selling Product Filter Records -->
        <record id="dynamic_filter_high_review_product" model="website.snippet.filter">
            <field name="filter_id" ref="custom_website.high_review_products_filter"/>
            <field name="field_names">name,display_name,description_sale,image_512,uom_id</field>
            <field name="limit" eval="16"/>
            <field name="name">High Review Products</field>
        </record>

    </data>
</odoo>